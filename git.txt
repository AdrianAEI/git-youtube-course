cd directory
git init
//glowny branch - main
copy con nazwa.txt
dir // wyswietla pliki w katalogu
code . //otwiera visual studio code dla danego katalogugit 
git config --global user.name "Adrian Pirog" 
git add -A 
git status
git log
git add nazwa.txt
git commit -m"komunikat"
git commit -am"wszystkie zmodyfikowane sa dodawane"
git checkout -b"logowanie" //new branch
git checkout master //switch branch
git push
